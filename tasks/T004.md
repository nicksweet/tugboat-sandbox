## Problem
README does not explain tier classification (simple/standard/complex) and intake expectations.

## File map
- **README.md** (Write)

## Deliverable
Add a "Tier expectations" section explaining simple/standard/complex tiers, intake gate behavior, and when agent-ready label is blocked per Constitution.

## Requirements
- [ ] Tier classification explained with signals
- [ ] Intake gate blocking behavior documented

## Agent execution protocol
1. **Diagnose** — read README.md to find insertion point after create-issue section
2. **Change** — add tier expectations section with tier table and blocking rules
3. **Self-check** — grep for "simple" "standard" "complex" and "block_agent_ready" in README.md
4. **Stop** — summarize changes

## Verification
```bash
grep -q 'simple' README.md && grep -q 'standard' README.md && grep -q 'complex' README.md && grep -q 'block_agent_ready' README.md
```

## Out of scope
- Modifying tier classification logic
- Adding configuration examples
