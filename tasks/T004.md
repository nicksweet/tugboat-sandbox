## Problem

OpenAPI client design documentation missing authentication page describing auth schemes, token handling, and credential management.

## File map

- **Write** `docs/openapi-client/auth.md` — OpenAPI client authentication design page

## Deliverable

Create auth.md documenting supported authentication methods, token refresh, and credential storage.

## Requirements

- [ ] docs/openapi-client/auth.md exists
- [ ] Documents auth schemes, token handling, and credential management

## Agent execution protocol

1. **Diagnose** — Confirm docs/openapi-client/ directory exists; verify auth.md missing
2. **Change** — Write auth.md with authentication design (API keys, OAuth, token refresh)
3. **Self-check** — Grep verify file exists and contains auth-related keywords
4. **Stop** — Summarize created authentication design content

## Verification

```bash
# Verify auth.md exists and has authentication content
ls -la docs/openapi-client/auth.md && grep -qi 'auth\|token\|credential\|oauth\|api.key' docs/openapi-client/auth.md
```

## Out of scope

- Overview or error handling details (separate pages)
- Modifying other design pages
- Python code changes
