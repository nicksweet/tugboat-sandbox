## Problem
README does not document how to run `tugboat create-issue` for agent-ready issue creation.

## File map
- **README.md** (Write)

## Deliverable
Add a "How to run tugboat create-issue" section with command syntax, --dry-run usage, and example workflow for validating and filing agent-ready GitHub issues.

## Requirements
- [ ] Create-issue command usage documented with syntax
- [ ] Create-issue --dry-run example included

## Agent execution protocol
1. **Diagnose** — read README.md to find insertion point after plan section
2. **Change** — add create-issue usage section with command reference and examples
3. **Self-check** — grep for "tugboat create-issue" and "--dry-run" in README.md
4. **Stop** — summarize changes

## Verification
```bash
grep -q 'tugboat create-issue' README.md && grep -q '--dry-run' README.md
```

## Out of scope
- Documenting intake gate internals
- Adding issue contract details
