## Problem

Repository lacks AGENTS.md to guide AI agents on project conventions, tooling, and contribution workflow.

## File map

- **Write** `AGENTS.md` — Root-level agent instructions document

## Deliverable

Create AGENTS.md with project-specific guidance for AI agents working in this repository.

## Requirements

- [ ] AGENTS.md exists at repository root
- [ ] Contains sections for project overview, tooling, and contribution workflow

## Agent execution protocol

1. **Diagnose** — Confirm AGENTS.md does not exist; read any existing contributing docs for reference
2. **Change** — Write AGENTS.md with project conventions, tooling (uv, pytest, tugboat), and agent workflow
3. **Self-check** — Grep verify AGENTS.md exists and contains expected section headers
4. **Stop** — Summarize created file and sections

## Verification

```bash
# Verify AGENTS.md exists and has required sections
ls -la AGENTS.md && grep -q '^# ' AGENTS.md && grep -q 'tooling' AGENTS.md && grep -q 'contribution' AGENTS.md
```

## Out of scope

- Modifying existing documentation files
- Adding automation or scripts
- Python code changes
