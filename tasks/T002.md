## Problem

Repository lacks a Mermaid diagram documenting system architecture for AI agents and human contributors.

## File map

- **Write** `MERMAID.mmd` — Architecture diagram in Mermaid format

## Deliverable

Create MERMAID.mmd with a system architecture diagram showing major components and data flows.

## Requirements

- [ ] MERMAID.mmd exists at repository root
- [ ] Contains valid Mermaid syntax for architecture diagram

## Agent execution protocol

1. **Diagnose** — Confirm MERMAID.mmd does not exist; review codebase structure for diagram content
2. **Change** — Write MERMAID.mmd with architecture diagram (graph TD or flowchart)
3. **Self-check** — Grep verify MERMAID.mmd exists and contains mermaid diagram markers
4. **Stop** — Summarize created diagram and components shown

## Verification

```bash
# Verify MERMAID.mmd exists and has mermaid diagram syntax
ls -la MERMAID.mmd && grep -q '^```mermaid\|^graph\|^flowchart' MERMAID.mmd
```

## Out of scope

- Rendering or publishing the diagram
- Modifying AGENTS.md or other docs
- Python code changes
