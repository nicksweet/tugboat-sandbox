# Spec: fix-readme-cli-examples

Correct README examples for `tugboat plan` and `tugboat create-issue` to match real CLI flags and sandbox E2E workflow.

## Acceptance

- [ ] Feature acceptance per task gates
