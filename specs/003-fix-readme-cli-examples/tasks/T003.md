## Problem

Operators testing Feature 007 queue need a README pointer to queue runbook docs and smoke-test commands.

## File map

- **Write** `README.md` — Add short queue E2E pointer section

## Deliverable

Add **How to run tugboat queue** section with dry-run and --once examples.

## Requirements

- [ ] README contains a queue section header
- [ ] README mentions `tugboat queue --dry-run`

## Agent execution protocol

1. **Diagnose** — Confirm README lacks queue section
2. **Change** — Add concise queue smoke-test commands
3. **Self-check** — Grep verify section and dry-run flag
4. **Stop** — Summarize

## Verification

```bash
grep -qi queue README.md && grep -q -- 'tugboat queue' README.md && grep -q -- '--dry-run' README.md
```

## Out of scope

- Full runbook content (Feature 005)
- Implementing queue command
