## Problem

README create-issue section uses JSON plan files and wrong flags. Sandbox E2E uses body files and `--dry-run`.

## File map

- **Write** `README.md` — Fix create-issue usage section

## Deliverable

Update README **How to run tugboat create-issue** with accurate CLI examples.

## Requirements

- [ ] README mentions `tugboat create-issue` with body file or brief
- [ ] README mentions `--dry-run` flag

## Agent execution protocol

1. **Diagnose** — Read README create-issue section
2. **Change** — Replace fictional JSON examples with tugboat intake CLI
3. **Self-check** — Grep for create-issue and dry-run
4. **Stop** — Summarize edits

## Verification

```bash
grep -q 'tugboat create-issue' README.md && grep -q -- '--dry-run' README.md
```

## Out of scope

- plan section (already fixed in T001)
- Filing GitHub issues automatically
