## Problem

README still shows fictional `tugboat plan --feature` and `tugboat create-issue --plan` syntax from early drafts. Replace with real flags documented in tugboat quickstarts.

## File map

- **Write** `README.md` — Fix plan usage section

## Deliverable

Update README **How to run tugboat plan** with correct CLI syntax.

## Requirements

- [ ] README mentions `tugboat plan --brief` or `--brief-file`
- [ ] README mentions `--project` and `--plan-only` flags

## Agent execution protocol

1. **Diagnose** — Read README plan section; note incorrect flags
2. **Change** — Replace with accurate tugboat plan examples
3. **Self-check** — Grep for --brief and --plan-only
4. **Stop** — Summarize README edits

## Verification

```bash
grep -q 'tugboat plan' README.md && grep -q -- '--brief' README.md && grep -q -- '--plan-only' README.md
```

## Out of scope

- create-issue section (T002)
- Python or config changes
