# Docs-only: add AGENTS.md, MERMAID.mmd, and docs/openapi-client/ design pages (overview, auth, errors). Split into separate tasks with at most 2 requirements and one write path each. No Python.

## Acceptance

- [ ] Feature acceptance per task gates
