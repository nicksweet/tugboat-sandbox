## Problem

Feature 007 queue operators need a runbook describing discovery, sort order, and dispatch flow.

## File map

- **Write** `docs/queue/runbook.md` — Queue operator runbook

## Deliverable

Create runbook with overview, dry-run, and --once workflow.

## Requirements

- [ ] docs/queue/runbook.md exists
- [ ] Runbook mentions plan tracker Status planned

## Agent execution protocol

1. **Diagnose** — Confirm docs/queue/ missing or empty
2. **Change** — Write runbook with queue command examples
3. **Self-check** — Grep verify file and key terms
4. **Stop** — Summarize

## Verification

```bash
test -f docs/queue/runbook.md && grep -qi 'tugboat queue' docs/queue/runbook.md && grep -qi planned docs/queue/runbook.md
```

## Out of scope

- Troubleshooting page (T002)
- Code changes in tugboat repo
